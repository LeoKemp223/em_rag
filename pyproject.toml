[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "em-rag"
version = "0.1.0"
description = "Embedded documentation RAG MCP server"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
include = ["em_rag*", "src*"]
